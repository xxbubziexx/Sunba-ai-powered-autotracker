# ── Virtual environment (project root is the venv) ────────────────────────────
Lib/
Include/
Scripts/
share/
pyvenv.cfg

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── Model weights (large files) ───────────────────────────────────────────────
*.pt
*.onnx
*.npy
models/

# ── Coral wheels (platform-specific, download separately) ─────────────────────
*.whl

# ── Training output ───────────────────────────────────────────────────────────
runs/

# ── Datasets (private annotated data) ─────────────────────────────────────────
dataset/
finetuning/

# ── Environment / secrets ─────────────────────────────────────────────────────
.env
